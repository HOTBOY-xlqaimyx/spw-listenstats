# 更新日志

**版本规则：任何修改都递增版本号。**

## 发版清单（每次版本变更必须走完）

1. 改 `build.gradle.kts` 的 `version`（`0.x` 阶段：修 bug 递增第三位，新增功能递增第二位）
2. 跑 `bash /vol2/1000/docker/spwbuild/release.sh` —— 等于：构建 + `selftest` + 发布到快速通道
3. 确认 `selftest` 全绿，记下产物 sha256 与字节数
   （假宿主集成测试已改为**可选**：`bash release.sh --with-hostsim`，仅在需要交叉验证时跑）
4. **把快速下载通道的链接与哈希贴给用户**（HTTP / SMB 两条）
5. 更新 `README.md` 顶部版本号、安装说明里的文件名、以及「快速下载通道」章节


- 当前**未正式上线**，版本号停留在 `0.x`；正式发布后才升到 `1.0.0`
- `0.x` 阶段：修 bug 递增第三位（`0.1.0` → `0.1.1`），新增功能递增第二位（`0.1.x` → `0.2.0`）
- 每次发版都要重跑 `selftest` + `hostsim` 两套验证，并同步更新下载通道

## 1.0.0 —— 首个正式版

从 0.1.0 到 1.0.0 共 16 个版本、210 项自测。1.0 的含义：

- **默认形态定型**：装上即用 —— 插件自己维护一份**本地报告**（单文件 HTML，默认在 `我的文档\SPW听歌统计`），
  **不依赖任何服务器**；NAS 上报与在线页是可选扩展。
- **数据可带走**：累计统计（`listenstats.json`）+ 逐条会话历史（`sessions.jsonl`）都在本地，
  拷走数据目录即完成搬迁。
- **上报协议稳定**：`spw.listenstats.v1`（后续只做增量字段，见 `plugin/DESIGN.md`）。
- **合规**：Apache-2.0；随包附 `LICENSE` 与 `NOTICE`（`lib/` 内含 kotlin-stdlib，已按许可证要求保留声明）。
- **仓库标准化**：monorepo 结构（`plugin/` `receiver/` `web/` `docs/`）+ `CONTRIBUTING` / `SECURITY` /
  `CODE_OF_CONDUCT` / Issue 与 PR 模板 / GitHub Actions CI（构建 + 自测 + 产出 zip）。
- 已知限制见根 README 的「已知限制」一节（宿主无切歌回调导致的「未归因」等）。

**1.x 版本规则**：不兼容变更 → 主版本；兼容新功能 → 次版本；修 bug / 文案 → 修订号。

## 0.9.5

**补上一个会让排障瞎眼的缺口，并加测试守住它**。

- **诊断的「配置原始值」清单原来是硬编码的**，0.9.x 新增的 `local.enabled` / `local.dir` / `report.pageUrl`
  **一个都没登记** —— 你按惯例把 `listenstats.log` 发来排障时，恰恰看不到这三个新配置。
- 把配置键收敛成**单一来源** `ConfigKeys.ALL`（纯 Kotlin，不碰宿主 API），诊断按它打印。
- **新增自测 `testConfigKeysMatchPreferences`**：读插件内置的 `preference_config.json`，
  断言「配置页声明的键 == `ConfigKeys.ALL`」——多一个少一个都报错。
  以后再加配置项忘了登记，`selftest` 会直接拦住（这类"加了忘登记"的坑就是这样防的）。
- 验证：`selftest` **210 项**。

## 0.9.4

**查缺补漏：修掉会话历史的两个真 bug，补齐诊断与文档**。

- **修 bug ①（数据丢失）**：`SessionLog.load()` 原来把整个循环包在一个 `runCatching` 里 ——
  一行 JSON 损坏（写入中断留下的半行）会让**后面所有历史都读不进来**。现在逐行容错：只跳过坏行，并告警「有 N 行无法解析」。
- **修 bug ②（无谓开销）**：`append()` 原来每追加一条就**整份重写**（2000 条 ≈ 500 KB，每首歌一次）；
  现在正常路径只 `appendText` 一行（≈250 B），仅当超过上限需要裁剪时才整份重写。
- **诊断补齐**：诊断块新增「本地报告: 开关 / 目录 / 最近刷新时间 / 待刷新」与「会话历史: N 条 + 文件路径」——
  以前排障时完全看不到本地报告的状态。
- **报告页 v0.4.1**：「最近播放」标注长曲目的**「分段」**（每 5 分钟切开的同一首，避免看着像重复播放），
  并在有未归因时段时说明「已计入总量、不进榜单」。
- **删死代码**：`ReportExport.exportSnapshot()`（带时间戳的快照导出）没有任何调用方。
- **文档补缺**：README 新增「本地报告（默认形态）」整节（位置/刷新时机/手机看/备份搬迁/已知局限）；
  `ARCHITECTURE.md` 数据流补 `sessions.jsonl` 与本地报告，并新增「输出形态（本地 vs 在线）」对照表；
  `RELEASE.md` 补「改了报告页要多跑一步」（页面必须用构建脚本生成二维码，且要同步插件内置模板并升插件版本）。

验证：`selftest` **205 项**（新增：裁剪后文件行数、追加写不重写（外部行仍在）、坏行只跳过自己、坏行有告警）。

## 0.9.3

**报告页 v0.4.0：能刷新，也能自己刷新**（用户：「报告页可以刷新，得到最新结果」）。

- 右上角新增 **⟳ 刷新按钮**（与夜间模式同一排的玻璃片）：
  - **在线页**：重新拉 `report-data.json?t=<时间戳>`（带缓存穿透），页面不重载、不闪；失败时保留上一次数据并只在 console 提示，不会白屏；
  - **本地报告**：走 `location.reload()` —— 重新读取磁盘上那份文件（插件 ≤30 秒就会自动重写它）。
- **在线页自动刷新**：每 **60 秒**拉一次，但**只在页面可见时**（`visibilityState`）；从后台标签切回来时若已过期立刻刷新一次。本地报告不做轮询（文件在本地，重载更快更准）。
- 「最后上报 X 分钟前」**每 30 秒自动重算**，页面挂一整天也不会一直显示"刚刚"。
- 页脚写明刷新方式（在线页：每 60 秒自动 + 可点 ⟳；本地报告：插件自动重写 + 按 ⟳ 重载）。
- 自测里那句"注入不破坏页面结构"不再绑定具体页面版本号（否则每次升页面版本都会被自己拦住）。

验证：CDP 实测 —— 在线页点 ⟳ **恰好触发 1 次 fetch** 且榜单仍在渲染；本地报告样本点 ⟳ **页面确实重载**（用标记被清掉证明，不是看代码）；
自动刷新间隔读出 `60000`；二维码仍可被 OpenCV 解码。

## 0.9.2

**示例文案换成通用地址**（用户：「例如中的例子不够大众」）。

- 原来示例里写的是**作者自己的内网地址**（`192.168.0.104`、`\\192.168.0.104\backup\spw`）—— 对别人、甚至换网段后的自己都是噪音。现在改成典型形态：
  - 上报地址：局域网自建 `http://192.168.1.100:8199/api/listen`；有域名也可 `https://stats.example.com/api/listen`
  - 本地报告保存位置：本地盘 `D:\听歌统计`，或已映射的网络盘 `Z:\听歌统计`
  - 在线报告页地址：`http://192.168.1.100:8123/spw-report/`
- 顺带把 `report.pageUrl` 的**默认值清空**（原来预填了作者的 NAS 地址）——
  现在插件包内 **0 处** 私有地址（已用 `grep -c 192.168.0.104` 对**发布产物**核验，不是看源码）。
- 代价：首次用「打开在线报告页」要自己粘一次地址（配置页说明里给了例子）。

## 0.9.1

**配置页文案优化**（用户：「优化下文本」）—— 去掉内部术语与历史理由，改成结论前置的人话：

| 位置 | 原来 | 现在 |
|---|---|---|
| 开关 | 统计无法归因的播放 | **未识别曲目的时长也计入**（"归因"是我们内部说法，用户读不懂） |
| 说明 | 宿主未给出曲目信息时…… | 插件启动时已在播的那首，宿主不会告知曲目 —— 这段时间仍计入总量与每日，但单独标为「未归因」，不进曲目榜 |
| 开关 | 生成本地报告（默认开） | **生成本地报告**（状态由开关表达，标题不写"默认开"） |
| 说明 | 会话结算后自动刷新一份自带数据的 HTML，放在「我的文档\…」…… | 听完一场就自动更新一份自带数据的 HTML 报告，双击就能看，不需要 NAS 或服务器 |
| 阈值 | ……避免滑块拖出畸形值 | 暂停或关歌超过这么久，就结算当前这一场 |
| 目录 | 本地报告目录（可选） | **本地报告保存位置** |
| 数据目录 | listenstats.json / report-queue.json / 日志都在这里 | 统计数据、**会话历史（sessions.jsonl）**、待发队列与日志都在这里 |
| 上报地址示例 | `…:8181/api/listen` | `…:8199/api/listen`（换成实际在用的端口） |
| 报告页 | 报告页地址 / 打开报告页 | **在线报告页地址** / **打开在线报告页**（与"本地报告"区分） |
| 上报时机 | 定时批量最省事、每曲结束时最实时 | 定时批量 = 攒一批发一次（省事）；每曲结束 = 听完一首立刻发（最实时）；仅退出 = 关软件时才发 |

验证：包内 `preference_config.json` 抽查 20 条文案全部就位；`README.md` 配置表同步；构建 + `selftest` 通过。

## 0.9.0

**「本地报告」变成默认形态，NAS/服务器降为可选扩展**（用户 2026-09-22 明确：*不叫离线报告，默认就是本地*）。

- **新增 `local.enabled`（默认开）+ 自动刷新**：插件启动、**每次会话结算后**（tick 节流，最多滞后一轮 30s）、
  插件停止时都会覆盖写**同一个文件** —— 不需要用户记得点按钮，永远有一份最新的。
- **落点好找**：默认 `我的文档\SPW听歌统计\ListenStats-本地报告.html`（依次探测 `Documents` → `OneDrive\Documents`
  → `Desktop` → 用户主目录）；`local.dir` 可改成同步盘或 SMB 共享目录（手机上用文件管理器直接打开）。
- **新增本地会话历史 `sessions.jsonl`**（滚动保留最近 2000 条）：本地报告新增「最近播放」逐条明细
  （时间 / 曲目 / 时长 / 结束原因），**在线页也显示同一区块**（接收端本来就返回 `recentSessions`）。
  为什么独立文件：逐条明细是追加型流水，混进 `listenstats.json` 会让每次落盘都变重。
- **按钮改为「刷新本地报告」/「打开本地报告」**（`打开本地报告` 没生成过就先自动生成再打开）。
- 数据即文件：整个数据目录（`listenstats.json` + `sessions.jsonl` + `report-queue.json`）
  拷走就等于搬家，**本地报告随即可用**。
- 报告页 v0.3.0：新增「最近播放」区块，文案从「离线快照」改为「本地报告」（数据来自插件本地统计，无需服务器）。

验证：`selftest` 200 项（+21：会话历史写入/上限裁剪/落盘恢复、本地报告数据含逐条明细、
目录解析（配置优先 + 自动探测）、固定文件名覆盖刷新、`</script>` 转义）；
线上页与本地报告两份都用 CDP 实测（本地报告样本放在**没有 report-data.json 的目录**里，仍能渲染 → 确实零依赖）。

## 0.8.0

**易用化：报告页会用了，插件也能脱离 NAS 单独用。**

- **新增「导出离线报告」按钮**：把**报告页 HTML + 当前统计**拼成一个**自包含文件**
  （`report-offline-YYYYMMDD-HHMM.html`，写在插件数据目录里）。双击就用浏览器打开，
  **不需要 NAS、不需要接收端、不联网** —— 数据本来就在插件自己的 `listenstats.json` 里，
  这才算真正做到"数据本地持久化、解绑服务器"。
  - 页面模板作为资源打进插件（`classes/web/report.html`），导出时用与接收端**同一套口径**聚合数据后内联。
  - 安全细节：内联 JSON 里的 `</` 会被转义成 `<\/`，否则曲目名里带 `</script>` 会让页面白屏。
- **新增「打开报告页」按钮 + `report.pageUrl` 配置**：省得记 URL，一键在浏览器里打开（`java.awt.Desktop`，
  运行时已确认可用）。纯本地便利功能，**不要求上报开着**。
- **报告页升到 v0.2.0**（线上与离线同一个文件）：
  - **时间范围**：每日时长支持「近 7 天 / 近 30 天 / 全部」，标题实时显示该区间合计；
  - **数据新鲜度**：标题下方显示「最后上报 X 分钟前」，>30 分钟变黄、>2 小时变红并提示"上报可能已断开"；
  - **曲目榜**：搜索框（曲目/艺人/专辑）+「展开全部」（Top10 ⇄ 55 首）；
  - **手机扫码**：页脚内置本页地址的二维码（部署时用 python-qrcode 生成内联 SVG，已用 OpenCV 解码验证可扫）。

验证：`selftest` 179 项（+15：离线数据口径、多艺人拆分、`</script>` 转义、模板注入、落盘产物）；
报告页用 CDP 实测四项交互 + 移动端夜间无横向溢出；二维码经 OpenCV `QRCodeDetector` 解码回本页 URL。

## 0.7.0

**「统计无法归因的播放」从「丢弃 / 记成未知曲目」改成「能归因就归因，真归不了才记未归因」**（用户决策 1）。

- **默认改成开**：这段是真实在听的时间，丢掉会让总量与每日对不上。
- **迟到的曲目信息会回填**：曲目身份只来自 `onBeforeLoadLyrics`，而插件启动时已在播的那首宿主不会补调。
  现在位置回调先到时先把时长挂在「占位会话」上，曲目信息一到就把这段**归因给这首曲目**，
  日志写「曲目信息迟到：此前 Ns 无主时长归因到 …」。
- **安全阀**：占位期间若位置出现回跳（很可能已经换歌），就不认领 —— 避免把 A 的时长算到 B 头上。
- **未归因独立成字段**：确实归不了的记 `totals.unattributedSeconds`（**计入总量与每日**，但**不进曲目表**），
  曲目榜/艺人榜里不会再出现一条巨大的「未知曲目」。
- 协议 `summary.unattributedSeconds`（0.7.0+，纯增量）；本地 `listenstats.json` 的 `totals` 同名持久化。
- 真机依据：5 天日志里 6 次「收到位置回调但尚无曲目信息」，位置已到 111.7s / 260.8s / 139.7s / 115.7s / 29.6s / 0s
  —— 都是插件重启后已在播的那首，此前这段时长全部丢弃。

验证：`selftest` 164 项（+15：迟到归因、回跳不认领、未归因独立记账、落盘往返、上报字段、默认值）。

## 0.6.2

**排障补强：首次触发日志带上 album / albumArtist，并显式标注空值**（为查清「44/45 首 albumArtist 为空」）：

- 新增纯函数 `describeMedia()`：空字段写 `(空)` 而不是留白 —— 这样一眼能分清
  「**文件标签里就没有 albumArtist**」和「**宿主没把 albumArtist 传过来**」，不用再猜。
  `onBeforeLoadLyrics` / `onAfterLoadLyrics` 的「回调首次触发」日志改用它的输出。
- 诊断块的「已有数据」行补 `摘要待补发=是/否`（0.6.x 的发送记账状态）：
  以后遇到「页面不更新」，看这一项就知道是不是有快照没送出去。
- 验证：`selftest` 149 项（+3：字段齐全 / 空值标注 / 全空不抛异常）。

## 0.6.1

**修 0.6.0 引入的日志刷屏**：定时任务里先去重再调用上报器 —— 之前只要「到了间隔且没有成功发送」，
每 5 秒的 tick 都会进一次上报器并打一行「队列为空且摘要已是最新，无需发送」。
`lastFlushAt` 只在发送成功后更新，所以这个条件会一直成立，日志会被刷爆（用户要整份发日志排障，必须干净）。
现在：`pending > 0 || 摘要比服务端新` 才调用。

验证：`selftest` 146 项（纯守卫逻辑，无新增用例）。

## 0.6.0

**修 0.5.0 的发布缺口：队列为空时也要把最新摘要推上去，手动按钮改为无条件发送**（用户报「页面数据不对」）。

- **问题现场**：真机上 0.5.0 装上后，服务端收到的最后一批仍是 0.4.0 发的，报告页曲目榜只有 15 首、艺人榜只有 1 人
  （本地实际 55 首）。原因：上报**只在「有会话入队」时才发**，而 `mode=interval` 下用户装上 0.5.0 后还没听完一首歌，
  队列一直是空的 → `summary.trackStats`（全量曲目聚合）永远发不出去；点「立即上报」也提示「队列为空」。
- **新增「摘要快照补发」**：本地记账 `reportState.summarySignature/sentVersion`（落盘在 `listenstats.json`）。
  只要**插件版本号变了**或**本地统计自上次上报后有变化**，即使队列为空也会发一份 `summaryOnly: true` 的报文
  （`sessions: []` + 完整 `summary`）。发成功后记账，不会每轮重复发。
- **触发点全部接上**：启动补发（升级后首次启动即推）、定时轮询、退出上报、以及**配置页「立即上报」按钮改为强制发送**。
- 报文新增 `summaryOnly` 字段（服务端可只用于日志；接收端 0.2.1 起打印 `summaryOnly=`）。
- 协议仍是 `spw.listenstats.v1`，纯增量。

验证：`selftest` 146 项（新增 16 项：空队列补发、无变化不重复发、统计变化后补发、强制发送、升级后补发、记账落盘）。

## 0.5.0

**上报里增加「全量逐曲目聚合」，让报告页的曲目榜/艺人榜拿到完整历史**（阶段 2 落地需要）：

- `summary` 新增 `trackStats`（按收听时长降序，最多 2000 条）与 `trackStatsTotal`。
  此前 `summary.tracks` 只是个**计数**，服务端只能靠 `sessions` 反推曲目 —— 而 `sessions` 只覆盖「开启上报之后」，
  真机上 74 次会话只有 12 次在覆盖范围内，曲目榜会大面积缺失。
- 协议仍是 `spw.listenstats.v1`：**纯增量**，旧接收端读到不认识的字段会忽略，0.4.0 的服务端不用改。
- 单条曲目字段：`title/artist/album/albumArtist/path/listenedSeconds/sessions/firstPlayedAt/lastPlayedAt`，
  与本地 `listenstats.json` 的 tracks 结构一致。
- 上限 2000 条（`StatsStore.MAX_REPORT_TRACKS`）兜住「曲库极大且每轮都上报」的请求体积；
  超出时截断但 `trackStatsTotal` 仍报全量，服务端不会把「榜上只有 2000 首」误当成全部。

验证：`selftest` 130 项（新增 12 项覆盖排序、字段完整性、截断与总数）。

## 0.4.0

**「启动自动上报」与「退出自动上报」做成显式开关，并用 JVM 退出钩子补上真正的退出缺口**（用户要求）：

- 新增配置 **`启动时自动上报`**（默认开）：插件启动若发现队列里有上次没发出去的记录，立刻补发一次。
  此前是 0.3.0 起的行为，现在**可关**。
- 新增配置 **`退出时自动上报`**（默认开）：
  - 插件停用（PF4J `stop()`）时补发一次；
  - **新增 JVM 退出钩子**：SPW 正常关窗退出时**不一定**会调 `stop()`（真机日志实测 7 次退出只有 1 次走到），
    而 JVM 会执行 shutdown hook —— 这样"退出自动上报"才覆盖多数退出方式。
  - 退出路径用**短超时**（连接 3s / 读取 5s），保证关软件不会被网络拖住；发送前后都落盘。
  - 局限（操作系统层面）：被任务管理器"结束任务"/SIGKILL 强杀时钩子也不会执行。
- 诊断块与日志都会写明：`启动上报=true 退出上报=true`，以及
  `启动补发 / 退出上报(JVM 退出钩子) / 退出上报(插件停止) / 上报(会话结束/定时/手动)` 的触发源与结果。

验证：`selftest` 118 项。

## 0.3.1

**两个时间控件从「滑块」改成「档位选择」**（用户反馈：滑块拖出来的值不精准 —— 实测其配置里出现过
`intervalMinutes = 4.36278` 这种畸形值，插件只能取整成 4 分钟）：

- `会话断开阈值` → 档位：**15 秒 / 30 秒 / 60 秒（推荐）/ 2 分钟 / 5 分钟**
- `定时上报间隔` → 档位：**1 / 2 / 5 / 10（推荐）/ 15 / 30 / 60 分钟**
- 兼容旧值：滑块时代留下的 `4.36278` 这类数值仍可正常读取（取整为 4 分钟），选一个档位后即被覆盖
- 顺手加固取值：抽出可单测的 `ConfigValues`，除了 `Number` 与纯数字字符串，还能解析**带单位的显示文本**
  （如 `"10 分钟（推荐）"` → 10、`"300 秒"` → 300）；解析不出才回退默认值

验证：`selftest` 115 项（新增 13 条配置解析用例）。

## 0.3.0

**「立即上报」真正立即**（用户要求）——原先只有配置页按钮能手动触发，且「每曲结束时上报」要等最多 30 秒的定时任务：

- **「每曲结束时上报」改为真·立即**：会话一结算并入队，立刻触发一次上报（不再等 30 秒定时）。
  实现方式：引擎在入队后回调插件，插件只把任务丢给定时线程——**绝不阻塞宿主的回调线程**；
  钩子抛异常也不会影响会话入库（有断言锁住）。
- **启动时自动补发积压**：插件启动若发现队列里有待上报数据（典型场景：上次是被直接关掉的进程、没走优雅退出），
  立刻发一次，不用等下一个定时周期。
- **「立即上报」按钮的提示更合理**：队列为空时给温和提示「当前没有待上报数据（统计与上报都正常）」，
  不再显示红色错误（以前会让人以为功能坏了）。
- 上报路径统一到一个 `flushInternal(触发源)`：日志里能明确看到是 `启动补发 / 会话结束 / 定时 / 手动` 哪一次发的，
  每条日志都带目标主机、模式、待发条数。

说明：配置页的「立即上报」按钮自 0.1.0 起就存在（第二组「上报到自己的服务端」最后一项），
本版改的是**时机与反馈**，不是新增按钮。

验证：`selftest` 102 项。

## 0.2.1

来自**真机日志**（Windows 11 / SPW 1.18.0 Steam / 约 19 分钟实测）的三项改进：

- **修：切歌瞬间的 0 秒会话不再刷屏**。宿主切歌时会先补一次旧曲目的位置回调，
  于是每次切歌都会产生一个 0 秒会话被丢弃，日志里出现 `丢弃过短会话 0s（trackChanged）`。
  数据本身没错（0 秒被丢弃、无重复计数），现在 0 秒不再打日志，1–4 秒的真实短播仍留痕。
- **增：缺失曲目时的明确诊断**。出现「有位置回调、但宿主还没给出曲目信息」时写一行：
  `收到位置回调但尚无曲目信息（position=…ms）：该段不会入库，直到宿主给出曲目（onBeforeLoadLyrics）…`
  每段只提示一次。真机日志显示这种情形的典型场景是「插件加载时那首歌已经在播」——
  实测一次会话里约 239 秒（~27%）因此未被统计。
- **增（默认关闭）：`stats.countUnknownTrack`** —— 开启后，上述无法归因的时间记入「未知曲目」桶
  （路径哨兵 `<unknown>`），总时长不再丢；默认关闭，行为与 0.2.0 完全一致。
  开启后若宿主随后给出曲目，则未知段立即结算、真实曲目另起一段（不合并，避免把上一首的时间记到下一首头上）。

另：真机复核确认 **Windows 版 SPW 的运行时同样没有 `java.net.http`**
（诊断块：`java.net.http.HttpClient=缺失 java.net.HttpURLConnection=可用`），
即 0.1.0 在 Windows 上也会初始化崩溃——0.1.0 的修复对 Windows 是必需的。

验证：`selftest` 97 项、`hostsim` 114 项。

## 0.2.0

**面向远程排障的日志增强**（用户只需把 `listenstats.log` 整份发出来即可定位问题）：

- 新增**诊断块**（启动、停止、手动点击时各写一次）：插件 id/版本/路径、宿主版本与渠道、OS/JVM/user.home、
  数据目录与可写性、配置文件路径、**配置原始值**（含宿主给的实际类型，能直接看出 Int/Float/Boolean 类型差异）、
  配置生效值、**宿主运行时探测**（`java.net.http.HttpClient` / `HttpURLConnection` / `Desktop` 是否可用）、
  已有数据量、上报器状态、回调计数
- 新增**回调逐类计数**：每类回调首次触发写一行带上下文日志（曲目/位置/状态），之后只计数；
  每 5 分钟一行 `回调统计: …`。这样「宿主根本没调我」「调了但没给曲目信息」「引擎没启动」三种情况一眼可分
- 新增 `onLyricsLineUpdated` 计数（不参与统计）：若它一直触发而 `onBeforeLoadLyrics` 从未出现，即可定位为宿主侧行为
- 新增**启动 60 秒无任何回调**的明确告警（最常见的「装了没反应」）
- 新增**tick 心跳**（每 60 次位置回调一行：位置/本会话时长/曲目）与会话**开始**日志
- 新增**落盘日志**：每次写盘记录 总计/会话/曲目/待上报
- 扩展点内部异常**改为写进日志文件**（原先只打到 stdout，用户发日志会漏掉）
- 配置页新增按钮 **「输出诊断信息」**（写日志并发 Toast 告知路径）
- ⚠️ 安全：诊断输出**不含上报令牌明文**、不含上报 URL 的路径与查询串（只给 scheme://host:port）；
  自测与假宿主都加了「令牌不得出现在日志里」的断言

验证：`selftest` 89 项、`hostsim` 106 项。

## 0.1.0

首个 0.x 版本（未上线）。功能：监听播放回调统计收听时长（会话切分、按天/按曲目累计、崩溃前定时落盘）、
声明式配置（7 项 + 3 个按钮）、可选 HTTP 上报（成功 / 失败重试 / 定时 / 仅退出四种模式）、
零第三方运行时依赖（只用 JDK 与 SPI API）。

本版本合并了开发过程中**三处实测发现的缺陷修复**（此前几个内部构建都叫 1.0.0，从未作为一个正式版本发布过）：

- **修：宿主根本加载不了插件**。PF4J 对「目录型插件」读的是 zip 解包后**顶层**的 `META-INF/MANIFEST.MF`
  （即外层打包 Jar 任务自己的清单）。原先只在内层 jar 写 `Plugin-*`，真实宿主会拿到空描述符
  （`id=null`、`class=org.pf4j.Plugin`）并报 `InvalidPluginDescriptorException: Field 'id' cannot be empty`。
  现在两个 Jar 任务写同一份清单，并加了「两份清单内容一致」的回归断言。
  *发现方式*：模拟宿主集成测试；*真机复核*：宿主日志 `Found descriptor … resolved`
- **修：听歌时长虚增**。原先只按播放位置增量累计，隐含「位置增量 ≈ 真实耗时」的假设：
  1 秒内补发 4000 次 tick 会被算成 3300 秒（55 分钟），倍速播放同样线性翻倍。
  现在每次取 `min(位置增量, 两次回调之间的墙钟增量)`。
  *发现方式*：模拟宿主并发风暴 + 自测的倍速/风暴用例
- **修：真机上插件初始化直接崩溃**。宿主是 jpackage/jlink 裁剪运行时，模块清单**没有 `java.net.http`**
  （实测 JDK 25：只有 `java.base java.desktop java.xml java.sql jdk.crypto.ec …`），
  原先用 `java.net.http.HttpClient` 发上报，在真实宿主里抛
  `NoClassDefFoundError: java/net/http/HttpClient`，**连本地统计一起被拖垮**。
  现改用 `java.net.HttpURLConnection`（java.base，任何运行时都有），
  并让「上报器构造失败」只禁用上报、不影响本地统计。
  *发现方式*：真实宿主实测（Salt Player 1.18.0 Linux + PF4J 3.14.1）

验证：`selftest` 79 项、`hostsim` 97 项全绿；真实宿主复核插件加载 / `resolved` / `Start plugin` / 扩展点实例化通过。
