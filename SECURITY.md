# 安全策略

## 支持范围

只有**最新发布版本**会被修复。请先升级到最新版再报告问题。

## 报告漏洞（请走私密渠道）

**不要开公开 Issue。** 请用 GitHub 的私密报告功能：

> 仓库页 → **Security** → **Report a vulnerability**

报告请尽量包含：版本号（插件 / Salt Player）、复现步骤、期望与实际行为、以及相关日志片段。

## 这个插件的数据边界（有助于判断风险）

- 插件**只**做两件与外界相关的事：
  1. 把统计上报到你**自己配置的服务端地址**（HTTP POST JSON，可在配置里关闭，默认关闭）；
  2. 按配置页的按钮打开**你自己的**报告页地址或数据目录。
- 上报内容**只有统计字段**：总量、按天、逐曲目聚合、会话明细（曲目名/艺人/专辑/文件路径/时长/时间）。
  **不包含**账号信息、音频内容、播放列表、任何凭据。
- 令牌（若你配置了）只作为 `X-SPW-Stats-Token` 请求头发送；**日志里只记"有/无"，不打印明文**。
- 插件**不收集、不上报**任何遥测；没有第三方服务器。
- 报告页 / 入口页是纯静态 HTML，**不加载任何外部脚本**，不联网（除同源读取 `report-data.json`）。

## 提 Issue 时的隐私提醒

`listenstats.log` 与 `report-data.json` 里会包含**你的文件路径与听歌记录**。公开贴出前请先脱敏；
上报地址若含内网 IP 或域名，也建议替换。
